//funcion que use include() para verificar si un array numero existe
const array = [1, 2, 3, 4, 5];
const includesThree = array.includes(0);
console.log(includesThree); //true si el array contiene el numero 0, false si no lo contiene
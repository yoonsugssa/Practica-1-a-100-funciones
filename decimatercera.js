//funcion que usa concat() para unir dos arrays
const primeros = [1, 2, 3];
const segundos = [4, 5, 6];
const combinados = primeros.concat(segundos);
console.log(combinados);
//combinados es el array unido